{
	"name": "pseuplex",
	"version": "0.0.1",
	"description": "A middleware for the plex server API",
	"main": "/dist/main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc",
		"start": "tsc && node dist/main.js --config-path=config.example.json",
		"lint": "eslint . --ext .ts"
	},
	"author": "",
	"license": "Zlib",
	"dependencies": {
		"express": "^4.19.2",
		"express-http-proxy": "^2.0.0"
	},
	"devDependencies": {
		"@types/express": "^4.17.21",
		"@types/node": "^20.14.11",
		"typescript": "^5.5.3"
	}
}
