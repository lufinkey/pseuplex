{
	"name": "pseuplex",
	"version": "0.0.1",
	"description": "A middleware for the plex server API",
	"main": "/dist/main.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc",
		"start": "tsc && node dist/main.js --config-path=config.example.json --verbose",
		"lint": "eslint . --ext .ts"
	},
	"author": "",
	"license": "Zlib",
	"dependencies": {
		"express": "^4.19.2",
		"express-http-proxy": "^2.0.0",
		"xml2js": "^0.6.2"
	},
	"devDependencies": {
		"@types/express": "^4.17.21",
		"@types/express-http-proxy": "^1.6.6",
		"@types/node": "^20.14.11",
		"@types/xml2js": "^0.4.14",
		"typescript": "^5.5.3"
	}
}
